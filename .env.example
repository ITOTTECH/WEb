# Optional local settings. Do NOT copy this file into public/.
# Empty for the root site; /itot-tech for a GitHub project site.
NEXT_PUBLIC_BASE_PATH=
NEXT_PUBLIC_SITE_ORIGIN=http://localhost:3000
# GitHub Actions supplies both values automatically from configure-pages.
# Never put secrets in NEXT_PUBLIC_* variables.
